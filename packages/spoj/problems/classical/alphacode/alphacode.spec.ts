/**
 * Test specs for alphacode problem
 * Spoj - Alphacode
 */

describe('Spoj - Alphacode', () => {
  describe('alphaCode', () => {
    describe('Example tests', () => {
      it('should work', () => {
        expect(1).toEqual(1);
      });
    });

    describe('Correctness tests', () => {
      it('should work', () => {
        expect(1).toEqual(1);
      });
    });

    describe('Performance tests', () => {
      it('should work', () => {
        expect(1).toEqual(1);
      });
    });
  });
});
